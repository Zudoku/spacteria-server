version: '2'
services:
  db:
    image: postgres
    volumes:
      - ./sqlscripts/init_tables.sql:/docker-entrypoint-initdb.d/init.sql
  web:
    build: .
    depends_on:
      - db
    ports:
      - "3590:3590"
